---
phase: 08-approval-workflows-unified-approval-queue
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - frontend/src/components/ApprovalQueue.tsx
  - frontend/src/App.tsx
  - frontend/src/components/__tests__/ApprovalQueue.test.tsx
  - frontend/src/pages/admin/SystemSettings.tsx
autonomous: true
requirements: [RBAC-04, RBAC-05]
user_setup: []

must_haves:
  truths:
    - "The unified Approval Queue page renders under the approval-queue tab (path /approval-queue) for Owner/Admin/Manager with tabs Appraisal | Redemption | Decision History, live pending-count badges, a search toolbar, and per-row Review/Approve/Reject actions; the old Appraisal Approval nav/route/render is removed from App.tsx while the ticket-creation PendingApprovalPanel flow stays intact (D-10). (RBAC-05)"
    - "Approve/Reject decisions are sent to the backend decide endpoints (POST /approval-queue/:id/approve and /reject) with the reject dialog enforcing a non-empty comment before Confirm Reject enables (D-09)."
    - "Queue renders distinct empty states per scope — 'All caught up!' (page), 'No pending appraisals'/'No pending redemptions' (tabs), 'No decisions yet' (audit) — and per-tab states render independently (tabs never hide). (UI-SPEC covered: empty, partial)"
    - "Queue shows 3 Skeleton rows while loading and an inline error card with Retry plus a toast on fetch failure. (UI-SPEC covered: loading, error)"
    - "Tab and header badge counts reflect pending counts live (zero-one-many); icon-only buttons carry aria-labels and status badges convey state by text AND color (a11y). (UI-SPEC covered: zero-one-many, a11y)"
    - "The redemption tab shows the threshold note reading redemptionApprovalThreshold from settings with a ₱50,000 fallback, and the owner can configure that threshold in System Settings via the existing PATCH endpoint with merge-on-write so finance/payroll keys are preserved. (RBAC-04/D-05/D-06/D-07/T-08-07)"
  artifacts:
    - frontend/src/components/ApprovalQueue.tsx
    - frontend/src/App.tsx (5 wiring points updated)
    - frontend/src/components/__tests__/ApprovalQueue.test.tsx (implemented green)
    - frontend/src/pages/admin/SystemSettings.tsx (threshold field + merged save)
  key_links:
    - "ApprovalQueue.tsx <-> backend GET /approval-queue + POST decide routes (08-02 contract): item shape and endpoints are locked by 08-UI-SPEC and the 08-01 RED test."
    - "App.tsx wiring <-> component: the approval-queue tab id must match TAB_TO_PATH / nav / FREE_ALLOWED_NAV / render so the page is reachable for Owner/Admin/Manager."
    - "SystemSettings save <-> PATCH /pawnshops/:id/settings: the branch-admin path must spread currentSettings BEFORE applying the new keys (Pitfall 7) or a threshold-only save wipes finance/payroll/ledger settings keys."
  backstops:
    - statement: "Long decision comments, appraisal notes, and customer names truncate to one line in queue rows and the Decision History table, with full text available in the review dialog."
      verification: backstop
    - statement: "The Decision History table scrolls horizontally (overflow-x-auto) on narrow viewports without breaking the layout."
      verification: backstop
---

<objective>
Build the unified Approval Queue page (frontend route /approval-queue) per 08-UI-SPEC — tabs for Appraisal | Redemption | Decision History, per-row approve/reject with required rejection comment, review dialog, ContractViewer handoff for appraisal approvals, and a persisted decision-history table — wire it into App.tsx, retire the Appraisal Approval nav/route/render (D-10), and add the redemption-threshold configuration field to System Settings with merge-on-write (D-05/D-06/D-07).

Purpose: RBAC-05's user-facing half. The backend (08-02) provides the permission-gated endpoint; this plan makes OWNER/ADMIN/Manager able to act on every pending appraisal and high-value redemption from one screen, and lets the OWNER configure the threshold that drives RBAC-04.

Output: a Gilded-Reserve-themed ApprovalQueue component, App.tsx nav/route wiring (approval-queue added, appraisal-approval retired, PendingApprovalPanel flow preserved), the 08-01 RED ApprovalQueue test implemented to green, and the threshold config field in SystemSettings.
</objective>

<execution_context>
@C:/Users/arvie/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/arvie/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-approval-workflows-unified-approval-queue/08-CONTEXT.md
@.planning/phases/08-approval-workflows-unified-approval-queue/08-UI-SPEC.md
@.planning/phases/08-approval-workflows-unified-approval-queue/08-PATTERNS.md
@.planning/phases/08-approval-workflows-unified-approval-queue/08-VALIDATION.md
@frontend/src/components/AppraisalApproval.tsx
@frontend/src/components/PendingApprovalPanel.tsx
@frontend/src/App.tsx
@frontend/src/lib/apiClient.ts
@frontend/src/lib/formatters.ts
@frontend/src/components/__tests__/ApprovalQueue.test.tsx
@frontend/src/pages/admin/SystemSettings.tsx
</context>

## Phase Goal (restated)

**Goal from ROADMAP:** Sensitive actions — creating an appraisal and releasing high-value redemptions — require OWNER/ADMIN sign-off, surfaced in one unified approval queue with a persisted, auditable decision trail.

This plan delivers the queue surface staff see and operate: one page that lists every pending appraisal and high-value redemption, approves/rejects them, shows who decided what, and hands approved appraisals into the existing contract-signing flow.

## Execution Notes

- **UI-SPEC is the contract.** Follow 08-UI-SPEC exactly: copywriting table, spacing (8-point, multiples of 4), typography (10px Syne micro labels, 14px DM Sans body, 18px Syne 800 gold amounts, 24px display title), colors (#C9A05C gold on #0A0A0F/#14141B/#1C1C26, #D44545 red destructive, #3DA86C green success, #D4A84B amber warning), accent reservation rules, and the vendored component list. Do NOT run shadcn init — every needed component is already vendored at frontend/src/components/ui/.
- **Component source analog:** AppraisalApproval.tsx is the canonical queue-page pattern (apiClient + Swal + toast + role gating); PendingApprovalPanel.tsx provides the Review/Approve/Reject button styling. Retire AppraisalApproval per D-10; keep PendingApprovalPanel usage intact (ticket-creation approval flow stays).
- **RED-to-GREEN:** the 08-01 ApprovalQueue.test.tsx scaffold is the executable contract; T2 implements it to green without loosening the assertions.
- **Backstop items** (long-text truncation, audit-table overflow) are held-out visual checks for /gsd-verify-work — implement the stated behavior now, mark them for the human visual pass.

## Delivery Inventory (this plan's files)

| File | Action | Task |
|------|--------|------|
| `frontend/src/components/ApprovalQueue.tsx` | Create: full queue page per UI-SPEC | T1 |
| `frontend/src/App.tsx` | Edit: 5 wiring points (import/TAB_TO_PATH/nav/FREE_ALLOWED_NAV/render) | T2 |
| `frontend/src/components/__tests__/ApprovalQueue.test.tsx` | Edit: implement scaffold to green | T2 |
| `frontend/src/pages/admin/SystemSettings.tsx` | Edit: threshold field + merged save | T3 |

## Tasks

<tasks>

<task type="auto">
  <name>Task 1: Build ApprovalQueue.tsx per 08-UI-SPEC</name>
  <files>frontend/src/components/ApprovalQueue.tsx</files>
  <read_first>frontend/src/components/AppraisalApproval.tsx (page pattern: role gating, apiClient fetch, Swal confirm, processing lock, Gilded Reserve containers), frontend/src/components/PendingApprovalPanel.tsx (Review/Approve/Reject button styling), frontend/src/lib/apiClient.ts (api.get<T>(path, query) / api.post<T>(path, body)), frontend/src/lib/formatters.ts (formatCurrency/formatDate/formatDateTime), frontend/src/App.tsx (useToast export), frontend/src/components/AppraisalApproval.tsx (ContractViewer sign -> Disburse handoff flow)</read_first>
  <action>
    Create frontend/src/components/ApprovalQueue.tsx implementing 08-UI-SPEC "Page Structure" top-to-bottom. Props: branchId, activeBranchId, userRole (per the App.tsx render contract). State: items, loading, error, activeTab, searchQuery, processingId, plus dialog state. Role gate: normalize userRole (uppercase, trim, spaces/dashes to underscores, BRANCH_ADMIN -> ADMIN) and compute canApprove as ['MANAGER','OWNER','ADMIN','SUPER_ADMIN'] mirroring AppraisalApproval.tsx:85-90; render the no-permission fallback (AlertTriangle + "Access Restricted" copy from PendingApprovalPanel) when the caller lacks approval.view_queue.

    Data: fetch via api.get<ApprovalQueueItem[]>('/approval-queue', { pawnshopId: branchId, type }) where type is 'APPRAISAL' for the appraisal tab, 'REDEMPTION' for the redemption tab, and status: 'DECIDED' for the Decision History tab (page-level default fetch covers pending across both types for the header count). Define a local ApprovalQueueItem interface per 08-UI-SPEC's item shape (id, targetType, targetId, ticketNumber, customer { fullName, contactNumber, loyaltyTier }, category, weight, itemCondition, appraisedValue, recommendedLoanAmount, riskScore, isHighRisk, appraisalNotes, amountPaid, threshold, requestedBy, createdAt, decisionComment). Swal confirm + api.post('/approval-queue/:id/approve', { decisionComment: '' }) / ('/approval-queue/:id/reject', { decisionComment }) with per-row processing lock (processingId disables that row's buttons and swaps the label for a gold Loader2 — existing pattern). Appraisal approve: Swal text "Approve {ticketNumber} and generate the loan contract?" (button "Yes, proceed"), then open ContractViewer for signing, then onSignComplete -> "Disburse Loan" (existing flow reused verbatim from AppraisalApproval). Redemption approve: Swal "Approve this redemption and release the ticket?" -> POST approve -> success toast "Redemption approved and released." Reject: dialog titled "Reason for rejection" with required Textarea (placeholder per copy contract) and Confirm Reject button disabled until non-empty; red (#D44545) destructive styling.

    Render structure (08-UI-SPEC section "Page Structure"): (1) header row with Display title "Approval Queue" (accent word gold) + micro-label subtitle "Review appraisals and high-value redemptions pending owner sign-off", pending-count pill (bg-[#14141B] card, gold 24px count), Refresh ghost button with Loader2 spin; (2) shadcn Tabs: Appraisal (gold Badge count) | Redemption (gold Badge count) | Decision History — tab labels use the micro-label voice, active tab gold; (3) toolbar: search input (Search icon left, placeholder "Search by customer name or ticket number…", gold focus ring) + threshold note on the redemption tab ("Redemptions above the ₱50,000 threshold require owner approval." — threshold read from the settings value returned per item, fallback display ₱50,000); (4) card-style queue rows (space-y-4): type badge (APPRAISAL/REDEMPTION), ticket number (gold micro label), customer name + loyalty tier badge (tierColors map), item summary (category — weight), amount (gold 18px formatCurrency), requestedBy + submitted date (formatDateTime), per-row [Review] [Approve] [Reject] (Review ghost, Approve gold, Reject red-ghost per PendingApprovalPanel); (5) loading: 3 Skeleton rows; error: inline error card + Retry + showToast(error.message, 'error'); empty: per-tab copy ("All caught up!" page / "No pending appraisals" / "No pending redemptions" / "No decisions yet"); (6) Review dialog (ui/Dialog max-w-lg #14141B): photo gallery with chevron nav (existing pattern), customer/item detail rows (label/value rgba(255,255,255,0.035) style), valuation block bg-[#C9A05C]/10 with gold amount + risk flag, appraisalNotes, amber-bordered previous rejection comment callout when present (D-11); redemption: ticket detail rows + amountPaid + threshold comparison; (7) Reject dialog (red border, AlertTriangle icon, required Textarea); (8) ContractViewer mount for appraisal approve; (9) Decision History tab (ui/Table wrapped in overflow-x-auto): columns Decision (Badge APPROVED green / REJECTED red / CANCELLED muted), Type, Reference, Amount, Approver, Comment (truncate 1 line), Decided At (newest first).

    Self-approval prevention: hide/disable the Approve button with muted copy when the current user id matches the item's requestedBy (discretion item from CONTEXT.md — implement if the user id is available via the api client / session; otherwise the backend guard is authoritative). Accessibility: aria-labels on icon-only buttons (photo-nav chevrons, refresh), badges carry text + color. All spacing multiples of 4 (no gap-1.5/half-step utilities); typography and colors strictly per UI-SPEC; amounts via formatCurrency (PHP en-PH). Do NOT create any new ui/ component — use only vendored components.
  </action>
  <verify>
    <automated>In frontend/: `npm run build` passes (component type-checks; not yet imported by App.tsx).</automated>
  </verify>
  <acceptance_criteria>
    ApprovalQueue.tsx implements the full UI-SPEC page structure with tabs, badges, search, review/reject dialogs, ContractViewer handoff, Decision History table, loading/error/empty states, role gating, and per-row processing lock; type-checks clean; uses only vendored components and existing patterns.
  </acceptance_criteria>
  <done>Queue component complete per UI-SPEC; build green; no new registry/component dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Wire approval-queue into App.tsx, retire appraisal-approval, implement the UI test to green</name>
  <files>frontend/src/App.tsx, frontend/src/components/__tests__/ApprovalQueue.test.tsx</files>
  <read_first>frontend/src/App.tsx (import line 57, TAB_TO_PATH ~131-162, nav item line 1205, FREE_ALLOWED_NAV ~1227-1240, render ~1619), frontend/src/components/__tests__/ApprovalQueue.test.tsx (08-01 RED scaffold — the contract), frontend/src/components/__tests__/AuctionQueue.test.tsx (Vitest mock pattern)</read_first>
  <action>
    Edit frontend/src/App.tsx at exactly the five verified wiring points (D-08/D-10):

    1. Import (line 57): replace the AppraisalApproval import with `import { ApprovalQueue } from './components/ApprovalQueue';`.
    2. TAB_TO_PATH (line 144): remove the 'appraisal-approval' entry; add 'approval-queue': '/approval-queue'.
    3. Nav item (line 1205): replace the appraisal-approval item with { id: 'approval-queue', label: 'Approval Queue', icon: ListChecks, roles: ['Owner', 'Admin', 'Manager'], type: 'OPERATIONAL' } positioned before audit-history (ListChecks must be imported from lucide-react if not already).
    4. FREE_ALLOWED_NAV (line 1232): replace 'appraisal-approval' with 'approval-queue'.
    5. Render (line 1619): replace the AppraisalApproval render with {activeTab === 'approval-queue' && <ApprovalQueue branchId={currentBranchId} activeBranchId={activeOperationalBranchId} userRole={userRole} />}.

    IMPORTANT (Pitfall 8): the ticket-creation approval flow — GET /pawn-tickets/pending-approval, its tab, and PendingApprovalPanel usage — stays fully intact (D-10 explicitly keeps it). Only the AppraisalApproval entries are retired.

    Then implement frontend/src/components/__tests__/ApprovalQueue.test.tsx to GREEN against the 08-01 scaffold, using the AuctionQueue.test.tsx pattern: mock the api client (or stub global fetch) and useToast. Assertions must keep the scaffold's contract: renders the "Approval Queue" title and the three tabs; the appraisal tab fetches GET /approval-queue with pawnshopId and type 'APPRAISAL' (redemption tab 'REDEMPTION'); approve posts to /approval-queue/:id/approve; the reject confirm is disabled until the comment is non-empty; "All caught up!" renders on an empty queue. Mock fetch responses with the ApprovalQueueItem shape so formatCurrency/formatDateTime rendering paths execute.
  </action>
  <verify>
    <automated>In frontend/: `npm test -- ApprovalQueue` passes; `npm run build` passes.</automated>
  </verify>
  <acceptance_criteria>
    Approval Queue appears in nav for Owner/Admin/Manager at /approval-queue; all five appraisal-approval wiring points removed; PendingApprovalPanel/ticket-creation flow untouched; ApprovalQueue.test.tsx green with the scaffold's assertions intact.
  </acceptance_criteria>
  <done>RBAC-05 UI reachable and tested; D-10 consolidation complete; build + UI tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Redemption threshold config in System Settings with merge-on-write</name>
  <files>frontend/src/pages/admin/SystemSettings.tsx</files>
  <read_first>frontend/src/pages/admin/SystemSettings.tsx (feature list ~180-193, handleConfirmSave 196-260, branch-admin path 219-250, how settings are read from supabase pawnshops row), frontend/src/lib/apiClient.ts (api.patch)</read_first>
  <action>
    Edit frontend/src/pages/admin/SystemSettings.tsx to let the pawnshop OWNER configure the RBAC-04 threshold through the existing settings endpoint (D-05/D-06/D-07 — no new settings surface):

    1. Add a numeric input in the branch-admin (non-super-admin) settings area: label "Redemption approval threshold (₱)" bound to a new state value (e.g. redemptionThreshold), initialized from the current settings object already fetched for the branch (the existing currentSettings read in handleConfirmSave, or the component's settings fetch — use the same source the feature flags use). Placeholder/empty value means "use default ₱50,000".
    2. In the branch-admin save path (handleConfirmSave, lines 219-250), change the merge so a threshold-only save CANNOT clobber existing keys (Pitfall 7 / T-08-07): build updatedSettings as { ...currentSettings, ...sanitizedConfig, redemptionApprovalThreshold: Number(redemptionThreshold), global_overrides: currentSettings.global_overrides || {} } — spreading currentSettings FIRST preserves finance/payroll/ledger and any other namespaced keys that the current wholesale replacement would wipe. Validate the threshold is a positive finite number (reject non-positive values with a toast before PATCHing).
    3. Do NOT change the super-admin global-overrides path (lines 200-218) — it already merges per-branch current settings.
    4. The backend service replaces the whole settings JSON (raw SQL SET settings = <payload>), so the frontend merge IS the protection — do not rely on the backend to merge.
  </action>
  <verify>
    <automated>In frontend/: `npm run build` passes; grep confirms the branch-admin save path spreads currentSettings before applying sanitizedConfig + the threshold key.</automated>
  </verify>
  <acceptance_criteria>
    OWNER can set the redemptionApprovalThreshold in System Settings; a save writes merged settings (existing finance/payroll/global_overrides keys preserved); non-positive thresholds rejected; super-admin path unchanged.
  </acceptance_criteria>
  <done>Threshold configuration surface live for OWNER with merge-on-write protection (D-05/D-06/D-07, T-08-07 mitigated).</done>
</task>

</tasks>

## Artifacts this phase produces

- `frontend/src/components/ApprovalQueue.tsx` — the unified queue page: header + pending pill + refresh, Tabs (Appraisal | Redemption | Decision History), search toolbar + threshold note, card rows with [Review][Approve][Reject], Review dialog, Reject dialog (required comment), ContractViewer handoff, Decision History table, loading/error/empty states, role gating + per-row processing lock + self-approval disable.
- `frontend/src/App.tsx` — import, TAB_TO_PATH entry, nav item, FREE_ALLOWED_NAV entry, render block: appraisal-approval retired, approval-queue added; PendingApprovalPanel flow untouched.
- `frontend/src/components/__tests__/ApprovalQueue.test.tsx` — implemented to green (tabs, fetch per tab, approve post, reject-comment gate, empty state).
- `frontend/src/pages/admin/SystemSettings.tsx` — threshold input + merged (read-modify-write) settings save.

## Interface Contract (consumed from 08-02)

- `GET /approval-queue?type=APPRAISAL|REDEMPTION&status=PENDING|DECIDED&pawnshopId=<branch>` -> ApprovalQueueItem[]
- `POST /approval-queue/:id/approve` { decisionComment? }
- `POST /approval-queue/:id/reject` { decisionComment: string (required non-empty) }
- `PATCH /tenant-governance/pawnshops/:id/settings` { settings: { ...currentSettings, redemptionApprovalThreshold } } — OWNER-accessible after 08-02 T3

## UI Considerations lift (08-UI-SPEC -> must_haves)

- covered -> must_haves.truths (empty / loading / error / zero-one-many / partial / a11y — see frontmatter).
- backstop (long-text, overflow) -> must_haves.backstops flat scalars with verification: backstop; the executor implements the stated behavior and flags them for the human visual pass in /gsd-verify-work.
- unresolved: none.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> queue API | Branch staff interact with the queue; frontend role gating is cosmetic — the backend @RequiresPermission guard is authoritative |
| browser -> settings API | Owner-supplied threshold value crosses here; must be validated client-side AND rely on backend service validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure / Tampering | ApprovalQueue UI role gating | medium | mitigate | canApprove normalization + no-permission fallback view mirrors the backend permission model, but the frontend gate is explicitly cosmetic; real enforcement stays in the 08-02 RbacGuard (backend authoritative — noted in plan and component) |
| T-08-07 | Tampering / DoS | SystemSettings threshold save | medium | mitigate | Merge-on-write (spread currentSettings first) so a threshold-only save cannot wipe finance/payroll keys (Pitfall 7); positive-number validation before PATCH (T3) |
| T-08-09 | Tampering | Reject comment bypass (UI) | low | mitigate | Confirm Reject disabled until comment non-empty; the backend decideApproval ALSO rejects empty comments (08-02) — UI gate is convenience, not security |
| T-08-10 | Spoofing | Self-approval via UI | low | accept | Backend guard (T-08-02, 08-02) is authoritative; UI disables Approve when the current user is the requester when the user id is available (discretion) — cosmetic only |

Note: no new npm/pip/cargo packages are installed this phase — package-legitimacy gate not applicable.
</threat_model>

<verification>
- Per-task: T1 `npm run build` (frontend); T2 `npm test -- ApprovalQueue` + `npm run build`; T3 `npm run build` + merge grep.
- Wave gate: full `npm test` in frontend/ green; backend suites unaffected (no backend files touched here).
- Manual (held-out, per 08-VALIDATION.md Manual-Only Verifications + UI-SPEC backstops): run `npm run dev`, log in as OWNER with approval.view_queue, open /approval-queue — verify tabs/empty/error states, badge counts, reject-comment gate, threshold note, and the ContractViewer sign -> disbursement handoff after approving an appraisal; verify long-text truncation and audit-table horizontal scroll on a narrow viewport.
- Security: T-08-01/T-08-07 mitigations are code-level; the backend remains the enforcement authority for all access control.
</verification>

<success_criteria>
- [ ] ApprovalQueue page reachable at /approval-queue for Owner/Admin/Manager with full UI-SPEC structure (T1)
- [ ] Appraisal Approval retired from nav/route/render; PendingApprovalPanel flow preserved (T2)
- [ ] ApprovalQueue.test.tsx green (tabs, fetch, approve post, reject-comment gate, empty state) (T2)
- [ ] Threshold configurable by OWNER in System Settings with merge-on-write (T3)
- [ ] Frontend build + full frontend test suite green
</success_criteria>

<output>
Create `.planning/phases/08-approval-workflows-unified-approval-queue/08-03-SUMMARY.md` when done
</output>
