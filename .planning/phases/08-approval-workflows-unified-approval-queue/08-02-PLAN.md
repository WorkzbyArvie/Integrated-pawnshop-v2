---
phase: 08-approval-workflows-unified-approval-queue
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - backend/src/loan/pawn-ticket.service.ts
  - backend/src/loan/pawn-ticket.service.spec.ts
  - backend/src/approval/approval.service.ts
  - backend/src/approval/approval.controller.ts
  - backend/src/approval/approval.module.ts
  - backend/src/approval/dto/decide-approval.dto.ts
  - backend/src/approval/dto/approval-queue-query.dto.ts
  - backend/src/app.module.ts
  - backend/src/common/guards/pawnshop.guard.ts
  - backend/src/common/permissions/permissions-catalog.spec.ts
  - backend/src/tenant-governance/tenant-governance.controller.ts
autonomous: true
requirements: [RBAC-03, RBAC-04, RBAC-05, RBAC-06]
user_setup: []

must_haves:
  truths:
    - "Submitting an appraisal leaves the ticket in PENDING_APPROVAL (never APPRAISED/OFFER_MADE) with the valuation captured in an ApprovalRecord(APPRAISAL, PENDING) payload; ticket.loanAmount is not modified at submission time — the D-02 write lands only on approval. (RBAC-03)"
    - "An in-person redemption whose amountPaid strictly exceeds the tenant's redemptionApprovalThreshold (settings JSON key, default 50000) creates a pending ApprovalRecord(REDEMPTION) and releases nothing; at-or-below-threshold redemptions run the full direct release unchanged. (RBAC-04/D-05/D-06)"
    - "GET /approval-queue returns the caller tenant's pending APPRAISAL and REDEMPTION records, newest first, with ticket/customer/requestedBy context and the previous rejection comment (D-11); reachable only with approval.view_queue. (RBAC-05/T-08-01/T-08-08)"
    - "Approve and reject both persist decidedById, decidedAt, decisionComment and the final status on the ApprovalRecord (RBAC-06/D-13); reject of an appraisal transitions the ticket back to RECEIVED (D-04) with no offer side effects; approve of a redemption runs the shared release (payment, ledger, LegalProof, receipt, tier, notification) (D-12); approve of an appraisal writes loanAmount from payload then runs the existing approveWithContract offer flow (D-03)."
    - "An approver cannot decide their own request (requestedById !== decidedById), a decided record cannot be re-decided, and decisions are scoped to the caller's pawnshop unless SUPER_ADMIN. (T-08-02/T-08-06/T-08-08)"
    - "Pawnshop OWNER can edit settings — including redemptionApprovalThreshold — through the existing PATCH /pawnshops/:id/settings (decorator broadened to tenant.manage); SUPER_ADMIN unchanged; ADMIN remains blocked at the guard (no tenant.manage in the Phase 7 catalog — deliberate, per user decision D-07). (T-08-07)"
  artifacts:
    - backend/src/approval/approval.module.ts
    - backend/src/approval/approval.service.ts
    - backend/src/approval/approval.controller.ts
    - backend/src/approval/dto/decide-approval.dto.ts
    - backend/src/approval/dto/approval-queue-query.dto.ts
    - backend/src/loan/pawn-ticket.service.spec.ts
    - backend/src/loan/pawn-ticket.service.ts (edited chokepoints + 4 new methods)
    - backend/src/app.module.ts (ApprovalModule registered)
    - backend/src/common/guards/pawnshop.guard.ts ('/approval-queue' exemption)
    - backend/src/common/permissions/permissions-catalog.spec.ts (3 MATRIX entries + 63->66 sites + updatePawnshopSettings entry)
    - backend/src/tenant-governance/tenant-governance.controller.ts (D-07 permission)
  key_links:
    - "ApprovalService <-> PawnTicketService: ONE-WAY dependency (ApprovalModule imports LoanModule, which exports PawnTicketService); chokepoint record creation lives INSIDE pawn-ticket.service.ts via Prisma directly — LoanModule never imports ApprovalModule, so no forwardRef/cycle."
    - "decideApproval side-effect ordering: applyApprovedAppraisal / releaseApprovedRedemption / rejectAppraisal run FIRST, then the ApprovalRecord decision is persisted LAST (record is the audit source of truth — Pitfall 9); a side-effect failure leaves the record PENDING and throws."
    - "Threshold gate reads ticket.pawnshop.settings (extended include) — never a client-supplied value; strict > comparison per Assumption A1."
    - "permissions-catalog.spec.ts scan: adding approval.controller.ts flips the 63-site count to 66 and requires the 3 MATRIX entries in the SAME commit as the controller (Pitfall 1); each site carries EXACTLY one permission (spec line 439)."
    - "D-07: updatePawnshopSettings MATRIX entry tuple ['SUPER_ADMIN'] -> ['OWNER'] because the spec asserts every tuple role holds the permission (line 441-444) and only OWNER (besides SUPER_ADMIN bypass) holds tenant.manage."
---

<objective>
Activate the dead ApprovalRecord model (RBAC-06) and wire the two approval chokepoints: appraiseTicket holds the ticket in PENDING_APPROVAL with a persisted approval task (RBAC-03/D-01/D-02), redeemTicket gates above-threshold redemptions behind OWNER/ADMIN approval (RBAC-04/D-05/D-06). Build the new backend/src/approval module exposing GET /approval-queue plus approve/reject decide endpoints (RBAC-05), with decisions persisted for the audit trail (D-13) and rejection routing appraisals back to RECEIVED for re-appraisal (D-04/D-11). Broaden the settings endpoint permission so the pawnshop OWNER can configure the threshold (D-07).

Purpose: This is the orchestration plan — all heavy machinery (approveWithContract, redeemTicket release body, StateMachineService, @RequiresPermission guard, settings pattern) already exists; the risk is wiring: one-way module dependencies, chokepoint record creation, extraction of the shared release path, and the catalog-spec count/MATRIX updates that must land atomically with the new controller.

Output: working GET /approval-queue + decide endpoints with tenant-scoped, permission-gated behavior; ApprovalRecord activated end-to-end; threshold gate live; OWNER settings editing enabled; the 08-01 RED specs for approval.service/controller now GREEN; permissions-catalog spec green at 66 sites; pawn-ticket.service.spec green.
</objective>

<execution_context>
@C:/Users/arvie/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/arvie/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-approval-workflows-unified-approval-queue/08-CONTEXT.md
@.planning/phases/08-approval-workflows-unified-approval-queue/08-RESEARCH.md
@.planning/phases/08-approval-workflows-unified-approval-queue/08-PATTERNS.md
@.planning/phases/08-approval-workflows-unified-approval-queue/08-VALIDATION.md
@backend/src/loan/pawn-ticket.service.ts
@backend/src/loan/pawn-ticket.controller.ts
@backend/src/loan/loan.module.ts
@backend/src/loan/dto/appraise-ticket.dto.ts
@backend/src/loan/dto/redeem-ticket.dto.ts
@backend/src/app.module.ts
@backend/src/common/guards/pawnshop.guard.ts
@backend/src/common/permissions/permissions.const.ts
@backend/src/common/permissions/permissions-catalog.spec.ts
@backend/src/common/state-machine/state-machine.service.ts
@backend/src/approval/approval.service.spec.ts
@backend/src/approval/approval.controller.spec.ts
@backend/src/tenant-governance/tenant-governance.controller.ts
@backend/src/tenant-governance/tenant-governance.service.ts
</context>

## Phase Goal (restated)

**Goal from ROADMAP:** Sensitive actions — creating an appraisal and releasing high-value redemptions — require OWNER/ADMIN sign-off, surfaced in one unified approval queue with a persisted, auditable decision trail.

This plan delivers the complete backend of that goal: the two chokepoints that CREATE approval tasks, the queue + decide endpoints that RESOLVE them, and the audit persistence underneath (RBAC-06).

## Execution Notes

- **Task ordering inside the plan is load-bearing:** T1 (pawn-ticket.service.ts methods) must land before T2 (approval module compiles against applyApprovedAppraisal/releaseApprovedRedemption/rejectAppraisal). T3 is independent of both and can be done last.
- **No new npm dependencies** — zero installs this phase (RESEARCH verified). Package-legitimacy gate not applicable.
- **RED-to-GREEN:** 08-01 created approval.service.spec.ts / approval.controller.spec.ts as the acceptance contract. T2 turns them green. Do NOT modify those spec files to fit the implementation — the implementation must fit the specs.
- **Catalog atomicity:** adding approval.controller.ts without the MATRIX/count updates breaks `npm test -- permissions-catalog` in one direction; updating the spec before the controller breaks it the other way. T2 must land controller + MATRIX + count in the same commit.

## Delivery Inventory (this plan's files)

| File | Action | Task |
|------|--------|------|
| `backend/src/loan/pawn-ticket.service.ts` | Edit: appraiseTicket chokepoint, redeemTicket threshold gate + shared release extraction, + applyApprovedAppraisal / releaseApprovedRedemption / rejectAppraisal | T1 |
| `backend/src/loan/pawn-ticket.service.spec.ts` | Create: chokepoint + threshold + new-method unit tests (green) | T1 |
| `backend/src/approval/dto/decide-approval.dto.ts` | Create | T2 |
| `backend/src/approval/dto/approval-queue-query.dto.ts` | Create | T2 |
| `backend/src/approval/approval.service.ts` | Create: getQueue + decideApproval | T2 |
| `backend/src/approval/approval.controller.ts` | Create: 3 guarded routes | T2 |
| `backend/src/approval/approval.module.ts` | Create: one-way module | T2 |
| `backend/src/app.module.ts` | Edit: register ApprovalModule | T2 |
| `backend/src/common/guards/pawnshop.guard.ts` | Edit: EXEMPT_PREFIXES + '/approval-queue' | T2 |
| `backend/src/common/permissions/permissions-catalog.spec.ts` | Edit: 3 MATRIX entries + 63->66 + updatePawnshopSettings entry | T2 + T3 |
| `backend/src/tenant-governance/tenant-governance.controller.ts` | Edit: D-07 permission broadening | T3 |

## Tasks

<tasks>

<task type="auto">
  <name>Task 1: Pawn-ticket chokepoints — PENDING_APPROVAL hold, threshold gate, shared release, approve/reject orchestration methods</name>
  <files>backend/src/loan/pawn-ticket.service.ts, backend/src/loan/pawn-ticket.service.spec.ts</files>
  <read_first>backend/src/loan/pawn-ticket.service.ts (appraiseTicket 364-439, redeemTicket 441-585, approveWithContract 249-362, assertPawnshopId 740-745, constructor 15-27), backend/src/loan/dto/appraise-ticket.dto.ts, backend/src/loan/dto/redeem-ticket.dto.ts, backend/src/loan/legal-proof.service.spec.ts (mock pattern)</read_first>
  <action>
    Five edits to backend/src/loan/pawn-ticket.service.ts, all at the verified chokepoints (D-01/D-02/D-03/D-04/D-12):

    EDIT 1 — appraiseTicket (364-439): keep the existing guard (lifecycleStatus must be RECEIVED — reject-to-reappraise returns here). Change the stateMachine.transition target from 'APPRAISED' to 'PENDING_APPROVAL' (line 380). In the ticket.update (384-392) set lifecycleStatus 'PENDING_APPROVAL', keep isHighRisk: (dto.riskScore ?? 0) > 40, and REMOVE the loanAmount write (line 388 — D-02: the loan amount is only applied on approval). Immediately after the transition, create an ApprovalRecord (chokepoint pattern, RESEARCH Pattern 1) with pawnshopId from assertPawnshopId(ticket), targetType 'APPRAISAL', targetId String(ticket.id), amount dto.appraisedValue, requestedById appraisedBy, status 'PENDING', and payload { appraisedValue, riskScore: dto.riskScore ?? 0, recommendedLoanAmount: dto.recommendedLoanAmount, itemCondition, appraisalNotes } cast to Prisma.InputJsonValue. Return lifecycleStatus 'PENDING_APPROVAL' instead of 'APPRAISED'. Keep the existing legalProofService.createProof and receiptService.generateReceipt calls (appraisal trail) — adjust only their summary text so it does not present loanAmount as final.

    EDIT 2 — redeemTicket (441-585) threshold gate (D-05/D-06): extend the ticket include (line 444) with pawnshop: { select: { settings: true } }. After assertPawnshopId (line 459) and BEFORE the stateMachine.transition, read the threshold: settings = (ticket.pawnshop?.settings as Record<string, unknown> | null) ?? {}; threshold = Number(settings['redemptionApprovalThreshold'] ?? 50000). If dto.amountPaid > threshold (strict, Assumption A1): create ApprovalRecord with pawnshopId, targetType 'REDEMPTION', targetId String(ticket.id), amount dto.amountPaid, requestedById processedBy, status 'PENDING', and return { ticketId, requiresApproval: true, approvalId, message: 'Approval required for high-value redemption' } with NO ticket/state/payment changes (D-12: ticket stays ACTIVE/GRACE_PERIOD; staff can re-request).

    EDIT 3 — extract the release body: move the redemption release sequence (stateMachine.transition ACTIVE/GRACE_PERIOD -> REDEEMED with userRole, ticket.update to REDEEMED, loan.update to REDEEMED, payment.create, financeService.createEntry + legalProofService.createProof, receiptService.generateReceipt, calculateAndUpdateCustomerTier, notification, and the return object — current lines 461-584) into a new private method performRedemptionRelease(ticket, loan, dto, processedBy, userRole?) that returns the same payload. redeemTicket's below-threshold path calls it. The approved path (releaseApprovedRedemption) MUST call this private method — never the public redeemTicket (that would re-enter the threshold gate and create a second approval record, i.e. a loop).

    EDIT 4 — new public methods for ApprovalService (D-03/D-12): applyApprovedAppraisal(ticketId: number, payload, decidedBy: string, userRole?) — load the ticket (include customer + pawnshop.legalEntity), verify lifecycleStatus is PENDING_APPROVAL (else BadRequestException), update the ticket with loanAmount: Number(payload.recommendedLoanAmount) and isHighRisk: (Number(payload.riskScore) ?? 0) > 40 (the D-02 write lands here), then call this.approveWithContract(ticketId, decidedBy, userRole) which performs the PENDING_APPROVAL -> OFFER_MADE transition and the full offer/contract/proof/notification flow. Do NOT call stateMachine.transition separately before approveWithContract — it transitions internally (pawn-ticket.service.ts:262-267) and a second call would throw Invalid transition. releaseApprovedRedemption(ticketId: number, dto, decidedBy: string, userRole?) — load the ticket with { customer, loans, pawnshop: { select: { settings: true } } }, validate lifecycleStatus is ACTIVE or GRACE_PERIOD and a loan exists (BadRequestException otherwise), then call performRedemptionRelease(ticket, loan, dto, decidedBy, userRole). rejectAppraisal(ticketId: number, userRole?) — load the ticket, verify lifecycleStatus is PENDING_APPROVAL, stateMachine.transition to 'RECEIVED' with userRole, then prisma.ticket.update lifecycleStatus 'RECEIVED' (D-04; stateMachine.transition must precede the direct update per the state-machine discipline in 08-PATTERNS.md).

    Create backend/src/loan/pawn-ticket.service.spec.ts — Jest TestingModule per the legal-proof.service.spec.ts pattern, mocking PrismaService (ticket/approvalRecord/loan/payment operations), StateMachineService.transition, and the injected legalProof/receipt/finance/notification/tier services (read the constructor at pawn-ticket.service.ts:15-27 for the exact injection list; mock each as jest.fn()). GREEN tests: (a) appraiseTicket creates the APPRAISAL record with the payload fields and leaves lifecycleStatus PENDING_APPROVAL without writing loanAmount (RBAC-03/D-02); (b) redeemTicket with amountPaid above the settings threshold creates the REDEMPTION record and returns requiresApproval true with no payment/ledger calls (RBAC-04); (c) redeemTicket at/below threshold calls performRedemptionRelease and returns REDEEMED; (d) applyApprovedAppraisal writes loanAmount from payload and delegates to approveWithContract; (e) rejectAppraisal transitions to RECEIVED; (f) releaseApprovedRedemption runs the release for ACTIVE/GRACE_PERIOD tickets.
  </action>
  <verify>
    <automated>In backend/: `npm test -- pawn-ticket` passes; `npm run build` compiles (verifies the new methods/types against the generated client).</automated>
  </verify>
  <acceptance_criteria>
    appraiseTicket no longer writes loanAmount at submission and holds PENDING_APPROVAL with an APPRAISAL record; redeemTicket gates on the tenant threshold and releases nothing above it; the release body is shared (single private method); applyApprovedAppraisal/releaseApprovedRedemption/rejectAppraisal exist and are unit-tested green; no circular dependency introduced (pawn-ticket.service.ts must not import ApprovalService).
  </acceptance_criteria>
  <done>RBAC-03/RBAC-04 chokepoints live with green unit specs; D-03/D-04/D-12 orchestration methods available for ApprovalService.</done>
</task>

<task type="auto">
  <name>Task 2: Approval module — service, controller, DTOs, module wiring, guard exemption, catalog spec update (atomic)</name>
  <files>backend/src/approval/approval.service.ts, backend/src/approval/approval.controller.ts, backend/src/approval/approval.module.ts, backend/src/approval/dto/decide-approval.dto.ts, backend/src/approval/dto/approval-queue-query.dto.ts, backend/src/app.module.ts, backend/src/common/guards/pawnshop.guard.ts, backend/src/common/permissions/permissions-catalog.spec.ts</files>
  <read_first>backend/src/approval/approval.service.spec.ts, backend/src/approval/approval.controller.spec.ts (the RED contracts from 08-01 — implement TO these, do not edit them), backend/src/loan/pawn-ticket.controller.ts (thin controller + @RequiresPermission + @AuditLog patterns, lines 1-139), backend/src/loan/loan.module.ts (exports PawnTicketService), backend/src/finance/finance.module.ts (module shape), backend/src/common/permissions/permissions.const.ts (approval.* permissions + holders), backend/src/common/permissions/permissions-catalog.spec.ts (MATRIX 31-284, site count 389-395, single-permission assertion 432-447), backend/src/common/guards/pawnshop.guard.ts (EXEMPT_PREFIXES 21-49), backend/src/app.module.ts (imports 32-53)</read_first>
  <action>
    Create the backend/src/approval module exactly to the 08-01 RED contracts:

    dto/decide-approval.dto.ts — class-validator: decisionComment?: string with @IsOptional @IsString @MaxLength(500) (reject requires non-empty — enforced in the service, ASVS V5).

    dto/approval-queue-query.dto.ts — type?: ApprovalTargetType (@IsOptional @IsEnum), status?: 'PENDING' | 'DECIDED' (@IsOptional @IsIn), pawnshopId?: string (@IsOptional @IsString).

    approval.service.ts — @Injectable, constructor injects PrismaService, PawnTicketService (from LoanModule — one-way), NotificationService. getQueue(query, callerPawnshopId): prisma.approvalRecord.findMany with where { pawnshopId: callerPawnshopId (tenant isolation), targetType: query.type, status: query.status === 'DECIDED' ? { in: ['APPROVED','REJECTED','CANCELLED'] } : 'PENDING' (default) }, orderBy { createdAt: 'desc' }, include requestedBy (profile). Batch-load the referenced tickets via prisma.ticket.findMany({ where: { id: { in: targetIds } }, include: { customer: true } }) and, for REDEMPTION items, the pawnshop settings for the threshold display value (mirror the finance.service.ts getPawnshopSettings pattern). Map to the queue item shape in 08-UI-SPEC (ticketNumber, customer fullName/contactNumber/loyaltyTier, category, weight, itemCondition, appraisedValue=amount, recommendedLoanAmount/riskScore from payload, appraisalNotes, requestedBy name, createdAt, decisionComment — the PREVIOUS rejection comment surfaced for the appraiser per D-11). decideApproval(id, dto, decidedBy, userRole, approve, callerPawnshopId): findUnique the record (NotFoundException if absent); BadRequestException if record.status !== 'PENDING' (TOCTOU, T-08-06); ForbiddenException if record.requestedById === decidedBy (self-approval, T-08-02 — if decidedBy is a Supabase auth id rather than a profile id, resolve the caller's profile id first via the existing authUserService pattern so the comparison is profile-to-profile); ForbiddenException if record.pawnshopId !== callerPawnshopId unless the caller is SUPER_ADMIN (cross-tenant, T-08-08); when approve=false, require a non-empty trimmed decisionComment (D-09, V5). Side effects FIRST, decision LAST (Pitfall 9): approve + APPRAISAL -> pawnTicketService.applyApprovedAppraisal(Number(record.targetId), record.payload as object, decidedBy, userRole); approve + REDEMPTION -> pawnTicketService.releaseApprovedRedemption(Number(record.targetId), { amountPaid: record.amount ?? 0, paymentMethod: undefined }, decidedBy, userRole) (amount from the record — never a fresh client amount); reject + APPRAISAL -> pawnTicketService.rejectAppraisal(Number(record.targetId), userRole); reject + REDEMPTION -> no ticket change (D-12). Then prisma.approvalRecord.update({ where: { id }, data: { status: approve ? 'APPROVED' : 'REJECTED', decidedById: decidedBy, decidedAt: new Date(), decisionComment: dto.decisionComment } }). Best-effort notification to the requester (requestedBy) via NotificationService reusing existing NotificationType enum values (no enum migration — RESEARCH A5), wrapped in try/catch that never fails the operation. Return the updated record.

    approval.controller.ts — three thin endpoints following pawn-ticket.controller.ts (no try/catch; typed exceptions propagate): GET approval-queue with @RequiresPermission(PERMISSIONS['approval.view_queue']); POST approval-queue/:id/approve with @RequiresPermission(PERMISSIONS['approval.approve_appraisal']) and @AuditLog('APPROVAL_APPROVE'); POST approval-queue/:id/reject with @RequiresPermission(PERMISSIONS['approval.approve_appraisal']) and @AuditLog('APPROVAL_REJECT'). Each decides/queues exactly ONE permission (the catalog spec asserts site.permissions equals a single-element array — line 439); approve_appraisal on both decide routes is behaviorally identical to approve_redemption because ONLY OWNER and ADMIN hold either permission (permissions.const.ts) — no role can approve an appraisal who cannot also approve a redemption. Read the caller context from req.user (id, role, pawnshopId per the Phase 7 guard shape) with a fallback to req.headers['pawnshop-id'] when user.pawnshopId is absent; pass user.id / user.role / pawnshopId into the service calls. @HttpCode(HttpStatus.OK) on all three.

    approval.module.ts — imports [PrismaModule, LoanModule, NotificationModule] (LoanModule exports PawnTicketService — verify loan.module.ts:48), controllers [ApprovalController], providers [ApprovalService]. One-way dependency: NO forwardRef anywhere.

    backend/src/app.module.ts — register ApprovalModule in the imports array (alphabetical placement between AuctionModule/AttendanceModule and BrandingModule, per 08-PATTERNS.md).

    backend/src/common/guards/pawnshop.guard.ts — add '/approval-queue' as a sibling of the existing '/pawn-tickets/pending-approval' entry in EXEMPT_PREFIXES (the queue path carries no pawnshop id in the route; tenant scoping is enforced in the service via the caller's pawnshop context — RbacGuard still enforces permissions).

    backend/src/common/permissions/permissions-catalog.spec.ts — SAME COMMIT as the controller (Pitfall 1): add three MATRIX entries — 'approval.controller.ts::getQueue' -> { tuple: ['OWNER','ADMIN','MANAGER','CASHIER_TELLER','APPRAISER'], permission: 'approval.view_queue' }, 'approval.controller.ts::approve' -> { tuple: ['OWNER','ADMIN'], permission: 'approval.approve_appraisal' }, 'approval.controller.ts::reject' -> { tuple: ['OWNER','ADMIN'], permission: 'approval.approve_appraisal' } — and bump the site-count assertion from 63 to 66 (line 394). The method names in the MATRIX keys must EXACTLY match the controller method names parsed by the scanner. KNOWN_TUPLES is unchanged (new sites carry no @Roles decorator); the 37-permission / 101-mapping counts are unchanged (approval.* already seeded). Every tuple role listed above holds its permission per the Phase 7 matrix — no new permission grants.
  </action>
  <verify>
    <automated>In backend/: `npm test -- approval` passes (08-01 RED specs turn GREEN); `npm test -- permissions-catalog` passes (66 sites); `npm run build` compiles.</automated>
  </verify>
  <acceptance_criteria>
    GET /approval-queue + approve/reject routes live, each gated by exactly one approval.* permission; queue results tenant-scoped, PENDING by default, DECIDED for the audit view; decideApproval persists decidedById/decidedAt/decisionComment/status for approve and reject, enforces self-approval/TOCTOU/cross-tenant guards and the reject-comment requirement; module wired one-way; catalog spec green at 66 sites; the 08-01 approval specs are GREEN unchanged.
  </acceptance_criteria>
  <done>RBAC-05 endpoint + RBAC-06 persistence live with green specs; no circular dependency; catalog machine-checked.</done>
</task>

<task type="auto">
  <name>Task 3: D-07 — broaden settings endpoint permission to tenant.manage (OWNER self-configures threshold)</name>
  <files>backend/src/tenant-governance/tenant-governance.controller.ts, backend/src/common/permissions/permissions-catalog.spec.ts</files>
  <read_first>backend/src/tenant-governance/tenant-governance.controller.ts (updatePawnshopSettings 377-386), backend/src/tenant-governance/tenant-governance.service.ts (updatePawnshopSettings ~2540-2566), backend/src/common/permissions/permissions.const.ts (tenant.manage holders), backend/src/common/permissions/permissions-catalog.spec.ts (MATRIX entry lines 196-199, permission-holder assertion 441-444)</read_first>
  <action>
    Per user decision D-07 (recorded in the plan-phase confirmation): the pawnshop OWNER must be able to self-configure the redemptionApprovalThreshold through the EXISTING settings endpoint — no new settings surface.

    Edit backend/src/tenant-governance/tenant-governance.controller.ts line 378: change @RequiresPermission(PERMISSIONS['platform.manage']) to @RequiresPermission(PERMISSIONS['tenant.manage']) on updatePawnshopSettings only. Leave every other controller method untouched (all remaining platform.manage sites stay SUPER_ADMIN-only).

    Update the matching MATRIX entry in backend/src/common/permissions/permissions-catalog.spec.ts ('tenant-governance.controller.ts::updatePawnshopSettings', lines 196-199): tuple ['SUPER_ADMIN'] -> ['OWNER'] and permission 'platform.manage' -> 'tenant.manage'. This is REQUIRED by the spec's holder-coverage assertion (every tuple role must hold the permission — line 441-444): OWNER holds tenant.manage; ADMIN and MANAGER do NOT (Phase 7 catalog), so ADMIN cannot edit settings via this endpoint. That is the deliberate, documented consequence of D-07 — the guard is now the authority, the service-level SUPER_ADMIN/OWNER/ADMIN assertion is looser but unreachable for ADMIN, which is safe (guard-stricter-than-service). Do NOT add tenant.manage to ADMIN's ROLE_PERMISSIONS — that would break the 101-mapping count and contradicts Phase 7's deliberate ADMIN-narrow design.

    Note for the executor: the service's raw-SQL wholesale settings replacement (tenant-governance.service.ts:2554) is intentionally NOT changed here — the merge-on-write protection for the threshold is delivered frontend-side in 08-03 T3 (read current settings, spread, then write) to keep this task small and avoid touching live raw SQL with unknown consumers.
  </action>
  <verify>
    <automated>In backend/: `npm test -- permissions-catalog` passes; `npm test -- tenant-governance` passes (existing tenant-governance.service.spec.ts untouched); grep confirms the controller decorator now references tenant.manage.</automated>
  </verify>
  <acceptance_criteria>
    PATCH /pawnshops/:id/settings reachable by OWNER (SUPER_ADMIN via bypass); ADMIN still 403 (no tenant.manage — documented); catalog spec green with the updated entry; no other endpoint permission changed; no catalog count changes.
  </acceptance_criteria>
  <done>D-07 delivered: OWNER self-configures the redemption threshold via the existing endpoint; catalog machine-checked.</done>
</task>

</tasks>

## Artifacts this phase produces

- `backend/src/approval/approval.module.ts` — NestJS module: imports PrismaModule + LoanModule + NotificationModule; controllers [ApprovalController]; providers [ApprovalService].
- `backend/src/approval/approval.service.ts` — class ApprovalService: getQueue(query, callerPawnshopId), decideApproval(id, dto, decidedBy, userRole, approve, callerPawnshopId).
- `backend/src/approval/approval.controller.ts` — class ApprovalController: getQueue (@approval.view_queue), approve (@approval.approve_appraisal + @AuditLog), reject (@approval.approve_appraisal + @AuditLog).
- `backend/src/approval/dto/decide-approval.dto.ts` — DecideApprovalDto { decisionComment? }.
- `backend/src/approval/dto/approval-queue-query.dto.ts` — ApprovalQueueQueryDto { type?, status?, pawnshopId? }.
- `backend/src/loan/pawn-ticket.service.ts` — edits: appraiseTicket (PENDING_APPROVAL + record creation, no loanAmount write), redeemTicket (threshold gate + pawnshop.settings include + shared release), new public methods applyApprovedAppraisal / releaseApprovedRedemption / rejectAppraisal, new private performRedemptionRelease.
- `backend/src/loan/pawn-ticket.service.spec.ts` — green unit suite for the chokepoints + new methods.
- `backend/src/app.module.ts` — ApprovalModule registered.
- `backend/src/common/guards/pawnshop.guard.ts` — EXEMPT_PREFIXES gains '/approval-queue'.
- `backend/src/common/permissions/permissions-catalog.spec.ts` — 3 approval.controller MATRIX entries, site count 63 -> 66, updatePawnshopSettings entry retargeted (['OWNER'] / tenant.manage).
- `backend/src/tenant-governance/tenant-governance.controller.ts` — updatePawnshopSettings decorator broadened to tenant.manage.

## Interface Contract (locked for downstream plan)

Locked for 08-03 (frontend):
- `GET /approval-queue?type=&status=&pawnshopId=` -> ApprovalQueueItem[] (shape in 08-UI-SPEC; REDEMPTION items include `threshold`)
- `POST /approval-queue/:id/approve` body { decisionComment? } -> updated record
- `POST /approval-queue/:id/reject` body { decisionComment (required non-empty) } -> updated record
- `PATCH /tenant-governance/pawnshops/:id/settings` body { settings } — now OWNER-accessible (D-07); service replaces the whole settings JSON, so the frontend MUST send merged settings (Pitfall 7 -> 08-03 T3)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff/branch app -> API | Untrusted input crosses here: approval decisions (id, decisionComment), queue filters, redemption amountPaid, settings payload |
| queue UI -> API | Branch-level staff (incl. non-approvers like MANAGER/APPRAISER with view_queue only) can read the queue but must not decide |
| approve/reject -> ticket lifecycle | Decision side effects advance or revert ticket state; must be atomic-ish and audit-persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure / Tampering | GET /approval-queue + decide routes | high | mitigate | @RequiresPermission(approval.view_queue / approval.approve_appraisal) + RbacGuard on every route (T2); service scopes every query and decision to the caller's pawnshopId (T2); frontend nav roles are cosmetic only — backend is authoritative |
| T-08-02 | Elevation of Privilege | decideApproval self-approval | medium | mitigate | Guard requestedById !== decidedById -> ForbiddenException (T2); profile-id normalization note; covered by RED spec |
| T-08-03 | Tampering | state-machine transitions | high | mitigate | All ticket transitions go through StateMachineService.transition (never bare prisma updates); ADMIN added to approval-relevant allowedRoles in 08-01; rejectAppraisal/applyApprovedAppraisal/releaseApprovedRedemption verify the current lifecycleStatus before acting (T1) |
| T-08-04 | Tampering | redeemTicket threshold gate | high | mitigate | Server-side gate compares dto.amountPaid against settings-stored threshold with strict > and default 50000 (T1); never trusts client-side amount; the approved path uses record.amount, not a new client value (T2) |
| T-08-05 | Repudiation | approval decisions audit | medium | mitigate | decidedById/decidedAt/decisionComment/status persisted on ApprovalRecord (RBAC-06, D-13) + @AuditLog on both decide routes + LegalProof from the shared release/offer flows (T1/T2) |
| T-08-06 | Tampering | TOCTOU double-decide | medium | mitigate | record.status !== 'PENDING' -> BadRequestException BEFORE any side effect (T2); status transition + decision write are the only mutations on the record |
| T-08-07 | Tampering / DoS | PATCH /pawnshops/:id/settings | medium | mitigate | Permission broadened only to tenant.manage (OWNER + SUPER_ADMIN bypass) per D-07 (T3); merge-on-write delivered frontend-side in 08-03 T3 so a threshold-only save cannot clobber finance/payroll keys; threshold validated as a positive number |
| T-08-08 | Tampering / IDOR | cross-tenant decision | high | mitigate | decideApproval and getQueue both enforce record.pawnshopId === caller pawnshopId unless SUPER_ADMIN (T2); queue route exempt from pawnshop-id header but scoped in the service (T2) |

Note: no new npm/pip/cargo packages are installed this phase — package-legitimacy gate not applicable.
</threat_model>

<verification>
- Per-task: T1 `npm test -- pawn-ticket` + `npm run build`; T2 `npm test -- approval` + `npm test -- permissions-catalog` + `npm run build`; T3 `npm test -- permissions-catalog` + `npm test -- tenant-governance`.
- Wave gate: full `npm test` in backend/ (targeted suites green; no regressions in rbac.guard/state-machine/catalog).
- DB-agnostic: all new specs use mocked Prisma; no live-DB dependency for the gate.
- Security: threat mitigations for T-08-01/04/08 (high) are code-level and asserted by the RED specs — the executor must not skip them.
</verification>

<success_criteria>
- [ ] RBAC-03: appraisal submission -> PENDING_APPROVAL + APPRAISAL record; no loanAmount write (T1)
- [ ] RBAC-04: threshold gate live, strict >, default 50000, REDEMPTION record, no release above threshold (T1)
- [ ] RBAC-05: GET /approval-queue + approve/reject routes live, permission-gated, tenant-scoped (T2)
- [ ] RBAC-06: every decision persisted with decidedBy/decidedAt/comment/status (T2)
- [ ] 08-01 RED specs green unchanged; permissions-catalog green at 66 sites; no new permissions/counts (T2)
- [ ] D-07: OWNER can edit settings via existing endpoint; ADMIN remains blocked (documented) (T3)
- [ ] No circular module dependency; zero new npm packages
</success_criteria>

<output>
Create `.planning/phases/08-approval-workflows-unified-approval-queue/08-02-SUMMARY.md` when done
</output>
