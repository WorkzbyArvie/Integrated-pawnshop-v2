---
phase: 09-kyc-verification-disbursement-guardrail
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - frontend/src/lib/kycDocs.ts
  - frontend/src/lib/kycDocs.test.ts
  - frontend/src/lib/types.ts
  - frontend/src/components/KycStatusBadge.tsx
  - frontend/src/components/__tests__/KycStatusBadge.test.tsx
  - frontend/src/components/BidderKycReview.tsx
  - frontend/src/components/CustomerKycReview.tsx
  - frontend/src/components/SalesPos.tsx
  - frontend/src/pages/admin/SuperAdminComplianceOverview.tsx
  - frontend/src/pages/admin/TrialRequestsPanel.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - KYC-02
  - KYC-05
must_haves:
  truths:
    - "OWNER/ADMIN/MANAGER open the Customer KYC Review screen from the sidebar and see the tenant's PENDING and All KYC submissions with live pending count."
    - "A reviewer opens a submission, views signed-URL document links, and sets VERIFIED (one click) or REJECTED (reason required); the list refreshes and the decision readout appears."
    - "Staff at the counter see the current customer's KYC status badge in SalesPos, can submit a new KYC capture (POST /kyc/customers) from a minimal inline form, and a blocked ticket creation surfaces the backend 409 message in a toast without clearing the form."
    - "The auction-bidder review screen (BidderKycReview) still renders documents — now via signed URLs, since the bucket is private (KYC-05/D-13)."
    - "Every kyc-documents read site in the dashboard renders via getSignedKycDocUrl: BidderKycReview, CustomerKycReview, SuperAdminComplianceOverview (idFront/idBack/selfie), TrialRequestsPanel (registration doc preview)."
    - "Producer-only getPublicUrl sites (AuctionMarketplace upload, PendingAccessDashboard upload) are classified unchanged — they form the stored URL string, they never render."
    - "Users without kyc.view/kyc.verify see the Access Restricted fallback, not the review surface (backend stays authoritative)."
  artifacts:
    - frontend/src/components/CustomerKycReview.tsx
    - frontend/src/components/KycStatusBadge.tsx
    - frontend/src/lib/kycDocs.ts
    - frontend/src/lib/kycDocs.test.ts
    - frontend/src/components/__tests__/KycStatusBadge.test.tsx
    - frontend/src/pages/admin/SuperAdminComplianceOverview.tsx (MOD — signed-URL doc renders)
    - frontend/src/pages/admin/TrialRequestsPanel.tsx (MOD — signed-URL doc preview)
  key_links:
    - CustomerKycReview calls GET /kyc/customers (kyc.view) and PATCH /kyc/customers/:id/review (kyc.verify) via the api client (auto pawnshop-id header)
    - getSignedKycDocUrl parses the stored object path from the saved public URL and signs it via supabase.storage.from('kyc-documents')
    - App.tsx nav wiring (customer-kyc tab → path → render) matches the backend MANAGER grant from 09-01
    - SalesPos capture form POSTs /kyc/customers (ungated per D-01) then refreshes the badge to PENDING
    - SuperAdminComplianceOverview / TrialRequestsPanel doc renders route through the shared kycDocs helper so the 09-04 bucket flip cannot 403 them
---

# 09-03 — Frontend: Customer KYC Review Screen + SalesPos Badge + Signed-URL Docs

> Wave 2. Depends on 09-01 (the `/kyc/customers` API contract and MANAGER grant). Frontend-only; zero files shared with 09-02/09-04. Implements the approved 09-UI-SPEC.md (2026-08-06) verbatim.

## Objective

Ship the tenant-scoped **Customer KYC Review** page (`CustomerKycReview.tsx`), the shared 4-state `KycStatusBadge`, the `kycDocs` signed-URL helper, flip ALL kyc-documents read sites to signed URLs (`BidderKycReview.tsx`, `SuperAdminComplianceOverview.tsx`, `TrialRequestsPanel.tsx`; bucket goes private in 09-04), wire the `customer-kyc` nav tab, add the minimal staff capture form in SalesPos (RESEARCH Open Question Q1 resolved — the capture surface now exists), and classify the two producer-only `getPublicUrl` sites as unchanged. SalesPos shows the current customer's badge and surfaces the backend gate-block 409 toast.

**Purpose:** KYC-02 frontend surface — OWNER/MANAGER/ADMIN review client KYC; staff visibility + capture via badge/form; documents stay viewable after the bucket flips private (KYC-05/D-13).
**Output:** New review page + badge + docs helper + capture form + 2 test files + 5 modified components/pages.

## Requirements

- **KYC-02** — OWNER/MANAGER review screen (D-04, D-06, D-07): list pending/all, review to VERIFIED/REJECTED with reason.
- **KYC-05** — all `kyc-documents` read sites render via signed URLs (D-13) so the private-bucket flip (09-04 SQL) does not break review screens; producer sites stay `getPublicUrl` (COVERAGE.md row 10).

## Verified Facts (read 2026-08-08)

- **09-UI-SPEC.md is an approved design contract** — spacing/typography/color/copy/component inventory/registry safety are LOCKED. This plan implements it; when in doubt, the UI-SPEC wins over this plan.
- `frontend/src/App.tsx` (1702 lines): nav item array `:1200-1232` (`approval-queue` `:1212`, `bidder-kyc` `:1221`); `TAB_TO_PATH` `:131+` (`bidder-kyc` `:137`); `FREE_ALLOWED_NAV` `:1234-1247`; render block `:1615+` (`bidder-kyc` render `:1635`). `useToast` exported `:108-109`; `api` from `./lib/apiClient` `:37`; `BidderKycReview` imported `:69`. Nav roles use display names (`'Owner'`, `'Admin'`, `'Manager'`).
- `frontend/src/lib/apiClient.ts` — `api.get<T>(path, query)` / `api.patch<T>(path, body)`; auto auth + pawnshop-id + branch-id headers (UI-SPEC Design System row). Frontend tests: **vitest ^3.2.4** (`npm test`), setup at `frontend/src/test/setup.ts`, component tests in `frontend/src/components/__tests__/`.
- `BidderKycReview.tsx` — three public `<a href>` doc links around lines 185-193 to flip to signed URLs (D-13); its list-row/dialog/approve-reject-with-reason pattern is the clone source for `CustomerKycReview.tsx`.
- `SalesPos.tsx` — customer section around lines 309-426 (name/contact/address inputs); ticket creation POSTs to the backend (Phase 5 wired) — 409 handling to be surfaced.
- `supabaseClient.ts` — `supabase.storage.from('kyc-documents')` available (UI-SPEC Storage row).
- Backend contract from 09-01: `GET /kyc/customers` (optional `status` query, `kyc.view`) returns `CustomerKyc[]` with `customer` include; `PATCH /kyc/customers/:id/review` body `{ decision: 'VERIFIED'|'REJECTED', rejectionReason? }` (`kyc.verify`). Gate-block is **409** with a clear message from 09-02 (D-08). `POST /kyc/customers` is ungated (D-01) — the SalesPos capture form calls it directly via `api.post` (apiClient.ts:147).
- Roles holding `kyc.view`/`kyc.verify` after 09-01: OWNER, ADMIN, MANAGER. STAFF/CASHIER_TELLER/APPRAISER do NOT — the SalesPos badge lookup is therefore rendered only for kyc.view holders (409 toast remains the universal staff safety net).
- **RESEARCH Open Question Q1 (KYC capture UI) — RESOLVED:** a minimal staff capture form is added to SalesPos (Task 3, this plan) calling `POST /kyc/customers`, and the 09-04 seed gains a PENDING demo row so the review flow is demonstrable. Not API-only; no user confirmation needed (staff-assisted capture is the phase boundary per D-01).
- **kyc-documents read-site sweep (verified 2026-08-08):**
  - `frontend/src/pages/admin/SuperAdminComplianceOverview.tsx` — super-admin tenant-compliance screen; the KYC dialog renders `viewingKyc.idFrontUrl` / `idBackUrl` / `selfieUrl` as `<img src>` inside `<a href>` at lines ~549-572 (6 public-URL usages to flip). NOTE: file is under `pages/admin/`, not `components/`.
  - `frontend/src/pages/admin/TrialRequestsPanel.tsx` — tenant-governance admin panel; `previewDoc.file_url` (registration docs uploaded by PendingAccessDashboard into `kyc-documents`) rendered via `<img>`, `<iframe>`, and `<a href>` at lines ~484-505. NOTE: file is under `pages/admin/`, not `components/`.
  - `frontend/src/components/AuctionMarketplace.tsx:515` and `frontend/src/components/PendingAccessDashboard.tsx:166` — `getPublicUrl` is used ONLY to form the stored URL string after upload (producer-only, COVERAGE.md row 10). NOT flipped; classified unchanged (Task 5).
- `KycIdType` enum (schema.prisma:892): NATIONAL_ID, PASSPORT, DRIVERS_LICENSE, SSS_ID, PHILHEALTH_ID, TIN_ID, VOTERS_ID, POSTAL_ID, OTHER — the capture form's idType select options.

---

## Tasks

<task type="auto">
  <name>Task 1: kycDocs signed-URL helper + KycStatusBadge + tests + BidderKycReview flip</name>
  <files>
    frontend/src/lib/kycDocs.ts (NEW)
    frontend/src/lib/kycDocs.test.ts (NEW)
    frontend/src/components/KycStatusBadge.tsx (NEW)
    frontend/src/components/__tests__/KycStatusBadge.test.tsx (NEW)
    frontend/src/components/BidderKycReview.tsx (MOD — doc links ~185-193)
  </files>
  <read_first>
    frontend/src/lib/supabaseClient.ts
    frontend/src/lib/apiClient.ts (patterns only)
    frontend/src/components/BidderKycReview.tsx (full — clone source + the ~185-193 doc-link block)
    frontend/src/components/__tests__/ApprovalQueue.test.tsx (existing test conventions: render, mock setup)
    frontend/src/test/setup.ts
  </read_first>
  <action>
    1. Create `frontend/src/lib/kycDocs.ts`:
       - `getSignedKycDocUrl(storedUrl: string, ttlSeconds = 3600): Promise<string>` — parse the stored object path from the saved public URL: take `new URL(storedUrl).pathname`, strip any leading `/` AND the bucket-name prefix if present (stored URLs may look like `https://<proj>.supabase.co/storage/v1/object/public/kyc-documents/<folder>/<file>` — reduce to `<folder>/<file>`; if the pathname already starts with the folder (no bucket prefix), use it as-is); then `const { data, error } = await supabase.storage.from('kyc-documents').createSignedUrl(path, ttlSeconds)`; throw `new Error('Unable to sign KYC document URL')` if `error` or no `data.signedUrl`; return `data.signedUrl`. No comments in source (AGENTS.md).
    2. Create `frontend/src/components/KycStatusBadge.tsx`:
       - Props `{ status: string | null | undefined }`. Map per UI-SPEC Color contract (4 states): VERIFIED → `bg-emerald-500/10 text-emerald-400` + CheckCircle (lucide, `h-3 w-3`); PENDING → `bg-[rgba(201,160,92,0.1)] text-[#C9A05C]` + Clock; REJECTED → `bg-red-500/10 text-red-400` + XCircle; NOT_SUBMITTED (or null/undefined) → `bg-[#1C1C26] text-[#6B655C]` + ShieldAlert. Pill classes: `inline-flex items-center gap-1 px-2 py-1 rounded-lg text-[10px] font-black uppercase tracking-widest` (UI-SPEC Typography micro label). Renders nothing (return null) when `status` is falsy? No — render the NOT_SUBMITTED pill for falsy (SalesPos badge must show a default state). Use a `STATUS_MAP` record keyed by status; `aria-label` with the human-readable status.
    3. Create `frontend/src/lib/kycDocs.test.ts` (vitest):
       - mocks `supabase.storage.from('kyc-documents').createSignedUrl`: full public URL with bucket prefix → called with the folder-relative path; already-relative path → passed through; error → rejects with `'Unable to sign KYC document URL'`.
    4. Create `frontend/src/components/__tests__/KycStatusBadge.test.tsx` (vitest + RTL):
       - renders 4 states with the right classes (assert presence of the state class token and the lucide icon by `aria-label`/`data-testid`) and NOT_SUBMITTED default for `null`.
    5. Modify `frontend/src/components/BidderKycReview.tsx` lines ~185-193: replace the three public `<a href={record.idFrontUrl...}>` links with signed-URL links — on render, resolve via `getSignedKycDocUrl` (per-link; a small `useState`/effect or an async map keyed by record id; simplest: a `DocLink` sub-component that fetches its signed URL once, shows the gold ExternalLink label while loading, and a muted disabled "Document unavailable" state when signing fails — UI-SPEC media row). `target="_blank" rel="noopener noreferrer"` on the anchor. NO other changes to BidderKycReview.tsx (D-05 — bidder flow stays untouched).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run kycDocs KycStatusBadge</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
    getSignedKycDocUrl signs/parses correctly per tests; badge renders all 4 states; BidderKycReview doc links are signed URLs with graceful failure — no other change to the bidder screen.
  </done>
</task>

<task type="auto">
  <name>Task 2: CustomerKycReview.tsx review page + shared type</name>
  <files>
    frontend/src/components/CustomerKycReview.tsx (NEW)
    frontend/src/lib/types.ts (MOD — add CustomerKycRecord type)
  </files>
  <read_first>
    frontend/src/components/BidderKycReview.tsx (clone source — list rows, dialog, approve/reject-with-reason, loading/error/empty states)
    frontend/src/lib/apiClient.ts (api.get / api.patch signatures)
    frontend/src/lib/formatters.ts (formatDate / formatDateTime)
    frontend/src/lib/types.ts (existing type style)
    09-UI-SPEC.md (Page Structure § CustomerKycReview — locked)
  </read_first>
  <action>
    Add to `frontend/src/lib/types.ts` a `CustomerKycRecord` type matching the 09-UI-SPEC review-item shape: `{ id: string; customerId: string; pawnshopId: string; fullName: string; contactNumber: string; address: string; idType: string; idNumber: string; idFrontUrl: string; idBackUrl?: string | null; selfieUrl?: string | null; status: 'PENDING' | 'VERIFIED' | 'REJECTED'; rejectionReason?: string | null; reviewedBy?: string | null; reviewedAt?: string | null; createdAt: string }` (plus `customer?: { id: string; kycStatus: string } | null`).

    Create `frontend/src/components/CustomerKycReview.tsx` implementing the UI-SPEC Page Structure exactly:
    - Props: `{ branchId?: number | null; activeBranchId?: number | null; userRole?: string | null }` (mirror ApprovalQueue's props shape — check ApprovalQueue.tsx).
    - Permission fallback: if `userRole` (normalized) is not Owner/Admin/Manager → render the Phase 8 "Access Restricted" fallback (AlertTriangle + copy from PendingApprovalPanel) and return early.
    - State: `records: CustomerKycRecord[]`, `tab: 'pending' | 'all'`, `loading`, `error`, `actionLoading`, `selected: CustomerKycRecord | null`, `rejectReason: string`. Derived `pendingCount`.
    - `load()`: `api.get<CustomerKycRecord[]>('/kyc/customers', tab === 'pending' ? { status: 'PENDING' } : undefined)`; sets records/error.
    - Header (UI-SPEC §1): h1 "Customer KYC Review" with gold "KYC" accent + micro-label subtitle; Pending pill (`text-[10px] font-black uppercase tracking-widest rounded-lg px-3 py-2`; active = `bg-[#C9A05C] text-[#0A0A0F]`, inactive = `bg-[#1C1C26] text-[#9B9488] hover:text-[#EAE2D6]`) with live count; All pill; Refresh ghost button (gold border/text, Loader2 spin while `loading`).
    - List (UI-SPEC §2): `space-y-2` card rows `p-4 rounded-xl bg-[#1C1C26] border border-[rgba(201,160,92,0.08)] hover:border-[rgba(201,160,92,0.2)] cursor-pointer`; status icon tile (`p-2 rounded-lg` + state palette from KycStatusBadge) — reuse `KycStatusBadge` or its palette; fullName `text-sm font-normal text-[#EAE2D6]` 1-line truncate; meta `text-[10px] text-[#9B9488]` `contactNumber · idType · Submitted {formatDate(createdAt)}`; Eye affordance right. Click opens dialog.
    - Loading / error / empty states per UI-SPEC §3-5 (exact copy strings from Copywriting contract: "No pending KYC submissions" / "No KYC records found" / "Failed to load KYC records" + Retry).
    - Review dialog (UI-SPEC §6): overlay `bg-black/60 backdrop-blur-sm`; surface `bg-[#14141C] border border-[rgba(201,160,92,0.15)] rounded-2xl max-w-4xl w-full max-h-[90vh] overflow-y-auto p-6`; header name `text-lg font-black text-[#EAE2D6]` + sub `{idType} #{idNumber}` + close × (aria-label); body `grid grid-cols-2 gap-6` — left "ID DOCUMENT" (10px 800 gold uppercase eyebrow) with 2-3 `DocLink`-style signed-URL links (View ID Front / View ID Back / View Selfie; gold, ExternalLink icon, `target="_blank" rel="noopener noreferrer"` — reuse the DocLink pattern from Task 1 via `getSignedKycDocUrl`); right "PERSONAL INFO" with contactNumber, address, submitted date. PENDING → action row `flex items-end gap-3`: **[Verify]** gold CTA (`px-4 py-2 bg-[#C9A05C] text-[#0A0A0F]` rounded, CheckCircle icon) → `api.patch('/kyc/customers/' + id + '/review', { decision: 'VERIFIED' })` then `showToast('KYC marked as verified.', 'success')`; rejection-reason Input (`bg-[#1C1C26] border border-[rgba(201,160,92,0.15)]`, placeholder "Rejection reason (required to reject)") + **[Reject]** red CTA (`bg-red-600 hover:bg-red-500`, XCircle icon, `disabled` until `rejectReason.trim()` non-empty) → `{ decision: 'REJECTED', rejectionReason }` then `showToast('KYC submission rejected.', 'success')`. Non-PENDING → readout per Copywriting ("Verified on {formatDate(reviewedAt)}" / "Rejected — {reason} on {formatDate(reviewedAt)}", semantic color). `actionLoading` disables both CTAs and swaps in Loader2. On action error: `showToast(err.message, 'error')` and keep the dialog open (UI-SPEC error row). After success: close dialog, `await load()`.
    - No comments in source (AGENTS.md). Use `useToast` from `../App` import path as in existing components (match the existing import style used by ApprovalQueue/BidderKycReview).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run</automated>
  </verify>
  <done>
    CustomerKycReview.tsx implements the UI-SPEC page (list, pills with live count, dialog, verify/reject-with-reason, signed-URL links, loading/error/empty, permission fallback) and compiles + full vitest suite green.
  </done>
</task>

<task type="auto">
  <name>Task 3: App.tsx nav wiring + SalesPos badge + minimal KYC capture form + 409 toast</name>
  <files>
    frontend/src/App.tsx (MOD — 4 anchor edits)
    frontend/src/components/SalesPos.tsx (MOD — badge + capture form + 409 toast)
  </files>
  <read_first>
    frontend/src/App.tsx (TAB_TO_PATH :131, nav array :1200-1232, FREE_ALLOWED_NAV :1234-1247, render block :1615+, imports :69)
    frontend/src/components/SalesPos.tsx (customer section ~309-426; ticket-creation POST call + its current error handling)
    frontend/src/components/ApprovalQueue.tsx (how it receives branchId/activeBranchId/userRole props)
  </read_first>
  <action>
    `App.tsx` — 4 edits (UI-SPEC Navigation changes, verbatim):
    1. Nav array: after the `approval-queue` entry (`:1212`), before `bidder-kyc` (`:1221`), add `{ id: 'customer-kyc', label: 'Customer KYC Review', icon: ShieldCheck, roles: ['Owner', 'Admin', 'Manager'], type: 'OPERATIONAL' }`.
    2. `TAB_TO_PATH` (`:131+`): add `'customer-kyc': '/customer-kyc'` near the other KYC entries.
    3. `FREE_ALLOWED_NAV` (`:1234-1247`): add `'customer-kyc'`.
    4. Render block (mirroring `approval-queue` at `:1625`): add `{activeTab === 'customer-kyc' && <CustomerKycReview branchId={currentBranchId} activeBranchId={activeOperationalBranchId} userRole={userRole} />}` and import `CustomerKycReview` beside the BidderKycReview import (`:69`).

    `SalesPos.tsx`:
    1. Render `<KycStatusBadge status={currentCustomerKycStatus} />` inline next to the customer-section field label area (UI-SPEC SalesPos surface; around the customer name/contact/address inputs `:309-426`). Resolve `currentCustomerKycStatus`: when `userRole` ∈ {Owner, Admin, Manager} (they hold `kyc.view`), fetch `GET /kyc/customers` (no status filter) once when the name+contact inputs are non-empty and debounced (existing debounce patterns in SalesPos if any — otherwise a simple `useEffect` on [name, contact] with a ~500ms timeout), and match the row whose `fullName`/`contactNumber` equal the typed values (normalize: trim + lowercase); take its `status`. For other roles (no `kyc.view`), skip the fetch entirely — the badge area renders nothing (the 409 toast is the universal gate signal per D-08). A fetch failure must NOT block the form — swallow silently.
    2. Minimal KYC capture form (RESEARCH Q1 resolved — the phase demo needs a way to produce PENDING records): in the customer section, add a collapsible "Capture KYC" panel (gold-titled micro-button beside the badge) with fields `idType` (select from `KycIdType` — NATIONAL_ID / PASSPORT / DRIVERS_LICENSE / SSS_ID / PHILHEALTH_ID / TIN_ID / VOTERS_ID / POSTAL_ID / OTHER), `idNumber` (text), `address` (prefill from the customer-section address input), and optional `idFrontUrl` / `idBackUrl` / `selfieUrl` (text URL inputs — demo capture; upload UX out of phase scope). On submit: `api.post('/kyc/customers', { fullName, contactNumber, address, idType, idNumber, ...(idFrontUrl && { idFrontUrl }), ...(idBackUrl && { idBackUrl }), ...(selfieUrl && { selfieUrl }) })` with fullName/contactNumber taken from the customer-section inputs (backend `resolveCustomerId` find-or-creates per 09-01); on success `showToast('KYC submission recorded as pending.', 'success')` and re-run the badge lookup so it flips to PENDING; on error `showToast(err.message, 'error')` and keep the form open with entered values (no reset). This is the only PENDING-producing surface in the phase UI; any authenticated staff role can call it (ungated per D-01).
    3. On ticket-creation 409: the existing POST error path must surface `err.message` via `showToast(err.message, 'error')` (UI-SPEC Copywriting — the backend message IS the contract; do NOT add a new copy string) and keep the form open with entered values (no reset).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `customer-kyc` nav item appears for Owner/Admin/Manager and routes to CustomerKycReview.
    - SalesPos renders the badge for kyc.view holders; capture form POSTs /kyc/customers and flips the badge to PENDING on success.
    - 409 from ticket creation surfaces the backend message in a toast without form reset.
  </acceptance_criteria>
  <done>
    `customer-kyc` appears in the sidebar for Owner/Admin/Manager and routes to the review screen; SalesPos shows the badge for kyc.view holders, submits KYC captures to PENDING, and surfaces backend 409 messages without form reset.
  </done>
</task>

<task type="auto">
  <name>Task 4: Flip SuperAdminComplianceOverview + TrialRequestsPanel doc rendering to signed URLs</name>
  <files>
    frontend/src/pages/admin/SuperAdminComplianceOverview.tsx (MOD — KYC dialog doc renders ~549-572)
    frontend/src/pages/admin/TrialRequestsPanel.tsx (MOD — doc preview ~484-505)
  </files>
  <read_first>
    frontend/src/pages/admin/SuperAdminComplianceOverview.tsx (KYC dialog block :540-577 — the `viewingKyc.idFrontUrl/idBackUrl/selfieUrl` renders)
    frontend/src/pages/admin/TrialRequestsPanel.tsx (preview modal :470-509 — `previewDoc.file_url` renders)
    frontend/src/lib/kycDocs.ts (the helper from Task 1 — `getSignedKycDocUrl`)
    frontend/src/components/BidderKycReview.tsx (the DocLink signed-URL pattern from Task 1 to mirror)
  </read_first>
  <action>
    `SuperAdminComplianceOverview.tsx` — the KYC dialog "Submitted Documents" block (lines ~549-572) renders `viewingKyc.idFrontUrl` / `idBackUrl` / `selfieUrl` as `<img src>` inside `<a href>` (6 public-URL usages). Convert to signed URLs: a small `SignedDocImage` sub-component (mirror the Task 1 DocLink pattern) that resolves `getSignedKycDocUrl(url)` once per URL — renders the `<img>` with the minted signed URL inside the `<a href={signedUrl}>` anchor; shows a muted skeleton/loading state while minting and a disabled "Document unavailable" state when signing fails. `target="_blank" rel="noopener noreferrer"` stays on the anchor. Import `getSignedKycDocUrl` from `../../lib/kycDocs`. NO other changes to the screen (super-admin compliance surface stays otherwise intact).

    `TrialRequestsPanel.tsx` — the doc-preview modal (lines ~484-505) renders `previewDoc.file_url` via `<img>`, `<iframe>`, and `<a href>` (registration docs stored in `kyc-documents` by PendingAccessDashboard's `registration-docs/` upload). Convert: resolve `getSignedKycDocUrl(previewDoc.file_url)` when the preview opens (async effect on `previewDoc`), then use the signed URL in all three render paths; keep the existing image/pdf/other branching on the ORIGINAL `file_url` (extension sniffing) but point `src`/`href` at the signed URL; show the loading state while minting and a disabled "Document unavailable" state when signing fails. Import `getSignedKycDocUrl` from `../../lib/kycDocs`.

    Both conversions are REQUIRED before the 09-04 bucket flip — RESEARCH Pitfall 2/3: post-flip, direct public URLs 403. No comments in source (AGENTS.md).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - SuperAdminComplianceOverview KYC dialog renders all 3 documents via signed URLs (no direct `<img src={viewingKyc.*Url}>` remains).
    - TrialRequestsPanel preview renders via signed URL for img/iframe/anchors.
    - Grep confirms no remaining kyc-documents public-URL renders in these two files.
  </acceptance_criteria>
  <done>
    Both admin read sites render kyc-documents via getSignedKycDocUrl with graceful failure; bucket flip (09-04) cannot 403 them.
  </done>
</task>

<task type="auto">
  <name>Task 5: Classify AuctionMarketplace + PendingAccessDashboard as producer-only (no code change)</name>
  <files>
    frontend/src/components/AuctionMarketplace.tsx (READ-ONLY — classification, NO modification)
    frontend/src/components/PendingAccessDashboard.tsx (READ-ONLY — classification, NO modification)
  </files>
  <read_first>
    frontend/src/components/AuctionMarketplace.tsx (uploadKycFile :499-517 — upload then getPublicUrl to form the stored URL string)
    frontend/src/pages/admin/PendingAccessDashboard.tsx (handleUploadDocument :146-183 — upload then getPublicUrl to form the stored URL string)
    COVERAGE.md row 10 (Public URL formation — INTEGRATE producer-only)
  </read_first>
  <action>
    Explicit classification task (COVERAGE.md row 10): both files are PRODUCER-ONLY `getPublicUrl` sites — they upload objects to `kyc-documents` and persist the returned public URL string as the STORED URL (AuctionMarketplace stores `idFrontUrl` via `POST /auth/kyc/submit`; PendingAccessDashboard stores `fileUrl` via `POST /tenant-governance/client-registrations/:id/documents`). They never RENDER kyc-documents URLs, so they are intentionally NOT flipped to signed URLs: after the bucket goes private, the stored public-URL strings remain the canonical stored value that `getSignedKycDocUrl` parses back to an object path (COVERAGE.md row 10, PATTERNS line 547). Make NO code changes to either file this phase.
    Document the classification in the plan SUMMARY (09-03-SUMMARY.md): site → producer-only rationale → COVERAGE.md row reference.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>grep -c "getPublicUrl" frontend/src/components/AuctionMarketplace.tsx frontend/src/components/PendingAccessDashboard.tsx — both present as producer sites (count &gt;= 1 each); grep confirms neither file renders kyc-documents URLs (no IMG-src or anchor-href pointing into the bucket — no render sites introduced)</automated>
  </verify>
  <acceptance_criteria>
    - AuctionMarketplace.tsx:515 and PendingAccessDashboard.tsx:166 remain getPublicUrl producer sites with zero render-site changes.
    - SUMMARY documents both as producer-only per COVERAGE.md row 10.
  </acceptance_criteria>
  <done>
    Both producer sites classified unchanged and documented; no kyc-documents render sites remain unflipped anywhere in the dashboard.
  </done>
</task>

---

<verification>
- **Automated:** `cd frontend && npx tsc --noEmit`; `npx vitest run` (full suite — existing SalesPos/InventoryVault/AuctionQueue/ApprovalQueue tests must stay green).
- **Read-site grep (Task 4/5):** confirm all four kyc-documents read surfaces (BidderKycReview, CustomerKycReview, SuperAdminComplianceOverview, TrialRequestsPanel) route through `getSignedKycDocUrl`; confirm AuctionMarketplace + PendingAccessDashboard remain producer-only `getPublicUrl` sites.
- **Live e2e demo** (after 09-02 + 09-04 merge, human-verified): sign in as OWNER/MANAGER → sidebar shows Customer KYC Review → PENDING tab shows the seeded PENDING row (09-04); SalesPos shows the gold PENDING badge after a capture submit; ticket creation for a NOT_SUBMITTED demo customer returns the 409 gate-block toast.
- Manual visual check of the review dialog against UI-SPEC (held-out: long rejection reasons, small viewport dialog scroll) is a post-execution UI-review item, not this plan's gate.
</verification>

<success_criteria>
- KYC-02 + KYC-05 frontend complete per the approved UI-SPEC: review screen, badge, capture form, signed-URL docs at ALL read sites, nav wiring, 409 toast.
- BidderKycReview changes limited to the signed-URL flip (D-05/D-13); producer-only sites (AuctionMarketplace, PendingAccessDashboard) untouched.
- Zero new dependencies; no `npx shadcn` calls (registry safety gate — all components vendored).
</success_criteria>

<output>
Create `.planning/phases/09-kyc-verification-disbursement-guardrail/09-03-SUMMARY.md` when done.
</output>

<execution_context>
- gsd-core/workflows/execute-plan.md + templates/summary.md
- Repo root: `Integrated-pawnshop-v2/`; frontend commands run with `cd frontend`.
</execution_context>

<context>
- 09-CONTEXT.md (D-04, D-05, D-06, D-07, D-08, D-13), 09-UI-SPEC.md (LOCKED contract), 09-RESEARCH.md (Open Question Q1 resolution), 09-VALIDATION.md, COVERAGE.md (rows 9-10 read-site/producer classification)
- Verified code: App.tsx (nav anchors), BidderKycReview.tsx, SalesPos.tsx (customer section + POST error path), SuperAdminComplianceOverview.tsx (KYC dialog :540-577), TrialRequestsPanel.tsx (preview modal :470-509), AuctionMarketplace.tsx (:515 producer), PendingAccessDashboard.tsx (:166 producer), apiClient.ts, supabaseClient.ts, types.ts, formatters.ts, existing __tests__ conventions
- The backend API contract comes from 09-01 (list/review endpoints + MANAGER grant); the 409 gate message comes from 09-02; the private-bucket flip comes from 09-04 (SQL).
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (review screen) → NestJS API | Untrusted; list/review are permission-gated server-side (09-01); frontend permission fallback is UX only |
| Browser → Supabase Storage | Signed-URL access only after bucket flips private (09-04 SQL); public-URL links removed here |
| SalesPos → /kyc/customers | Read-only, kyc.view holders only; failure must not block the sale form |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-20 | Information Disclosure | Signed-URL links (CustomerKycReview / BidderKycReview / SuperAdminComplianceOverview / TrialRequestsPanel) | medium | mitigate | `getSignedKycDocUrl` uses `createSignedUrl` with 1h TTL; `rel="noopener noreferrer"`; signing failure renders a disabled state, never a dead public href (D-13, UI-SPEC media rows) |
| T-09-21 | Spoofing | Review UI misrepresenting status/decision | low | mitigate | Status badge and readout render solely from API data; decision actions call the backend, which re-validates state (09-01 non-PENDING guard) |
| T-09-22 | Tampering | KYC decision without kyc.verify | medium | mitigate | PATCH is backend-gated (09-01 `@RequiresPermission`); the frontend hides Verify/Reject behind the role fallback but never trusts it |
| T-09-23 | Information Disclosure | Badge lookup leaking records to STAFF | low | mitigate | Badge fetch only for kyc.view holders; other roles get no badge — backend 409 remains the universal gate |
| T-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new dependencies (lucide-react, sweetalert2, @supabase/supabase-js already in package.json); no `npx shadcn add` per registry safety gate |
</threat_model>
